#pragma once
#include <iostream>
#include <fstream>
#include <string>

using namespace std;

string hashKey = "yzbqklnj";

//Finds and returns the lowest digit that produces a hash that starts with 5 zeros.
int FindSuitableHash(int method)
{
	int currentDigit = 1;

	string currentHash = "";

	return currentDigit;
}